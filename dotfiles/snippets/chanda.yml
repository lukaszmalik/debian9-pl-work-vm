---
db:live:
  do: copy
  desc: DB live 
  command: mysql -uparr -p<<pass>> pearson  -A -P<<port(3306)>>; 
db:aslive:
  do: copy
  desc: DB aslive 
  command: mysql -uparr -p<<pass>> pearson  -A -P<<port(3306)>>; 
tunel:live:
  do: exec
  desc: tunel DB live 
  command: ssh -L <<port(3306)>>:parr-db.dev.mmgapi.net:3306 <<user(lukaszmalik)>>@<<ip>>
tunel:aslive:
  do: exec
  desc: tunel DB aslive 
  command: ssh -L <<port(3306)>>:aslive-parr-db.dev.mmgapi.net:3306 <<user(lukaszmalik)>>@<<ip>>
