super + w ; u;
    xdotool getwindowfocus windowsize 50% 70% ; xdotool getwindowfocus windowmove 5% 10%

super + w ; w;
    xdotool getwindowfocus windowsize 70% 80% ; xdotool getwindowfocus windowmove 15% 10%;

super + w ; super + w;
    xdotool getwindowfocus windowsize 100% 100% ; xdotool getwindowfocus windowmove 0 0 ;

super + w ; h;
    xdotool getwindowfocus windowsize 50% 100% ; xdotool getwindowfocus windowmove 0 0;

super + w ; l;
    xdotool getwindowfocus windowsize 50% 100% ; xdotool getwindowfocus windowmove 50% 0;

super + c;
    wmctrl -xa terminator || terminator

super + f;
    wmctrl -xa Firefox || firefox

super + t;
    wmctrl -xa Telegram || telegram-desktop

alt + F2;
    gmrun

alt + q;
    wmctrl -c :ACTIVE:

alt + m;
    xdotool getwindowfocus windowminimize

